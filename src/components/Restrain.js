import React from 'react'
import { Box } from 'rebass'
import styled from 'styled-components'

const Restrain = styled(Box)`
  max-width: 70em;
  margin-left: auto;
  margin-right: auto;
`

export default Restrain
